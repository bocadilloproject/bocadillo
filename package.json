{
  "name": "bocadillo",
  "version": "0.15.1",
  "scripts": {
    "build:apiref": "pydocmd generate",
    "start": "npm run build:apiref && vuepress dev docs",
    "build": "npm run build:apiref && vuepress build docs"
  },
  "dependencies": {
    "date-fns": "^1.30.1",
    "dotenv": "^8.0.0",
    "vuepress": "^0.14.11"
  }
}
